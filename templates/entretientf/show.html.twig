{% extends 'base.html.twig' %}

{% block title %}Entretient{% endblock %}

{% block body %}
    <h1>Entretient</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entretient.id }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ entretient.date ? entretient.date|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ entretient.type }}</td>
            </tr>
            <tr>
                <th>Resultat</th>
                <td>{{ entretient.resultat }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_entretientf_index') }}">back to list</a>

    <a href="{{ path('app_entretientf_edit', {'id': entretient.id}) }}">edit</a>

    {{ include('entretientf/_delete_form.html.twig') }}
{% endblock %}
